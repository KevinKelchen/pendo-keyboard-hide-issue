import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pendo-keyboard-hide-issue',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
